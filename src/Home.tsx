import React from "react";
import Button from "./components/Button";

const Home = () => {
  return (
    <div>
      Home component
      <Button onClick={() => {}} text="Click Home" />
    </div>
  );
};

export default Home;
